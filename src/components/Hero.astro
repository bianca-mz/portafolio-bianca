<section class="hero">
  <div class="hero-text">
    <h1 class="font-cursive">Bianca Sánchez</h1>
    <p>
      ¡Hola! Soy desarrolladora de software junior. Me dedico a crear 
      aplicaciones y páginas web donde el código sólido se da la mano 
      con un diseño bonito y fácil de usar.
    </p>
    <a href="#proyectos" class="btn">Proyectos →</a>
  </div>

  <div class="hero-avatar">
    <img src="/icon_web.png" alt="Avatar ilustrado de Bianca" />
  </div>
</section>

<style>
  .hero {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 3rem 4rem;
    gap: 1rem;
  }

  .hero-text {
    max-width: 55%;
  }

  .hero-text h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    color: var(--color-text-dark);
  }

  .hero-text p {
    font-size: 1rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--color-text-dark);
  }

  .highlight-pink {
    color: #d63384;
    font-weight: 600;
  }

  .highlight-blue {
    color: #0d6efd;
    font-weight: 600;
  }

  .btn {
    display: inline-block;
    padding: 0.5rem 1.5rem;
    border: 2px solid var(--color-text-dark);
    border-radius: 999px;
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--color-text-dark);
    transition: background 0.2s ease, color 0.2s ease;
  }

  .btn:hover {
    background: var(--color-text-dark);
    color: white;
  }

  .hero-avatar {
    width: 300px;
    height: 300px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid white;
    flex-shrink: 0;
    background: white;
  }

  .hero-avatar img {
    width: 125%;
    height: 125%;
    object-fit: cover;
    object-position: top;
  }
</style>